// The generated config content depends on the project module type, so this
// action is exercised against every example that represents a distinct module
// system (CJS / ESM / TypeScript).
export default ['commonjs', 'esm', 'typescript']
