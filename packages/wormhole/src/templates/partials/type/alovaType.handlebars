{{~#with this~}}{{#if (not type "typescript")}}import('alova').{{/if}}Alova<{{#if (eq alovaVersion "v3")}}{{#if (not type "typescript")}}import('alova').{{/if}}AlovaGenerics{{else}}any, any, any, any, any{{/if}}>{{~/with~}}
