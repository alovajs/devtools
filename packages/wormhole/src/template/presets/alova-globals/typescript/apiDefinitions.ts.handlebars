{{> comment}}
{{#if config.useImportType}}import './globals.d.ts';{{else}}/// <reference types='./globals.d.ts' />{{/if}}

const apiDefinitions = {
{{#each tagedApis}}
  {{#apis}}
  '{{{pathKey}}}': ['{{method}}', '{{{path}}}'],
  {{/apis}}
{{/each}}
} as const;

export default apiDefinitions;
