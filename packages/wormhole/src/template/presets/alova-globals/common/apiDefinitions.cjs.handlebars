{{> comment}}
{{#if config.useImportType}}/// <reference types='./globals.d.ts' />{{else}}/// <reference types='./globals.d.ts' />{{/if}}

const apiDefinitions = {
{{#each tagedApis}}
  {{#apis}}
  '{{{pathKey}}}': ['{{method}}', '{{{path}}}'],
  {{/apis}}
{{/each}}
};

module.exports = apiDefinitions;
