{{> comment}}
import ky from 'ky';

export const kyInstance = ky.create({
  baseUrl: '{{{baseUrl}}}/',
  hooks: {
    beforeRequest: [
      _state => { },
    ],
    afterResponse: [
      _state => { },
    ],
  },
});
