{{{commentText}}}
import { createApis, createAxiosInstance } from './{{createApisFileName}}';

export const axiosInstance = createAxiosInstance('{{{baseUrl}}}');

export const Apis = createApis(axiosInstance);

export default Apis;
