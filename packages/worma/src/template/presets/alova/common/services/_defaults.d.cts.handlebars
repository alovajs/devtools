import type { ServicesMap } from '../helper.cjs';

type TagDefaultConfig<T extends keyof ServicesMap> = {
  [K in keyof ServicesMap[T]]?: Partial<Parameters<ServicesMap[T][K]>[0]>
};

{{#each tagedApis}}
export const {{{tagName}}}DefaultConfig: TagDefaultConfig<'{{{tagName}}}'>;
{{/each}}
