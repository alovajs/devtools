{{> comment}}
import * as userConfig from './index';
import type { ServicesMap } from '../helper';

type TagDefaultConfig<T extends keyof ServicesMap> = {
  [K in keyof ServicesMap[T]]?: Partial<Parameters<ServicesMap[T][K]>[0]>
};

{{#each tagedApis}}
type _{{{tagName}}}Resolved = typeof userConfig extends { {{{tagName}}}DefaultConfig: infer V } ? V : TagDefaultConfig<'{{{tagName}}}'>;
export const {{{tagName}}}DefaultConfig = (
  (userConfig as typeof userConfig & { {{{tagName}}}DefaultConfig?: TagDefaultConfig<'{{{tagName}}}'> }).{{{tagName}}}DefaultConfig ?? {}
) as _{{{tagName}}}Resolved;
{{/each}}
