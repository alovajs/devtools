{{> comment}}
{{#each tagedApis}}
import type * as {{{tagName}}} from './{{{tagName}}}';
{{/each}}

type MethodDefaultConfig<ApiMap extends Record<string, (config: any) => any>> = {
  [K in keyof ApiMap]?: Partial<Parameters<ApiMap[K]>[0]>;
};

{{#each tagedApis}}
export const {{{tagName}}}DefaultConfig: MethodDefaultConfig<typeof {{{tagName}}}> = {};
{{/each}}
