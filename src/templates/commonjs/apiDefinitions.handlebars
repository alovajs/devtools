///<reference path='./globals.d.ts' />
{{{commentText}}}
/**
 * @typedef {'{{#each pathsArr}}{{this.key}}{{#unless @last}}' | '{{/unless}}{{/each}}'} ApiEndpointKey
 */
/**
 * @type {Record<ApiEndpointKey, [string,string]>}
 */
module.exports = {
{{#pathsArr}}
  "{{{key}}}": ["{{method}}", "{{{path}}}"],
{{/pathsArr}}
};