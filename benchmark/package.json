{
  "name": "worma-benchmark",
  "type": "module",
  "private": true,
  "description": "Benchmark comparing worma with other OpenAPI TypeScript code generators",
  "scripts": {
    "generate-specs": "tsx scripts/generate-specs.ts",
    "bench": "pnpm generate-specs && tsx bench.ts",
    "dev": "pnpm generate-specs && concurrently -n vite,hono -c blue,green \"cross-env NODE_OPTIONS=--expose-gc vite\" \"cross-env NODE_OPTIONS=--expose-gc tsx server.ts\"",
    "dev:vite": "cross-env NODE_OPTIONS=--expose-gc vite",
    "dev:server": "pnpm generate-specs && cross-env NODE_OPTIONS=--expose-gc tsx server.ts",
    "build": "vite build",
    "start": "pnpm generate-specs && cross-env NODE_OPTIONS=--expose-gc tsx server.ts --prod",
    "preview": "vite preview",
    "clean": "rm -rf output node_modules dist results specs"
  },
  "devDependencies": {
    "@ant-design/icons-vue": "^7.0.1",
    "@hey-api/client-fetch": "^0.8.4",
    "@hey-api/openapi-ts": "^0.64.0",
    "@hono/node-server": "^1.14.0",
    "@vitejs/plugin-vue": "^6.0.0",
    "ant-design-vue": "^4.2.6",
    "concurrently": "^9.1.0",
    "cross-env": "^7.0.3",
    "echarts": "^5.6.0",
    "hono": "^4.7.0",
    "openapi-typescript": "^7.6.0",
    "tsx": "^4.22.4",
    "typescript": "^5.9.3",
    "vite": "^7.3.1",
    "vue": "^3.5.17",
    "vue-echarts": "^7.0.3",
    "wormajs": "latest"
  }
}
