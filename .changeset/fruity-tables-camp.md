---
"@alova/wormhole": minor
---

1. Extend the `apifox` fetch plugin; align defaults to Apifox’s official exported interface defaults (breaking change).
2. Fix the `payloadModifier` plugin not working for intersection and union types.
3. In the `rename` plugin, add a `value` parameter to `transtransform`.
